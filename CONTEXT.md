# Apidock

Apidock 是一个纯离线、基于本地 SQLite 数据库存储的 API 管理工具，用 Rust 构建，对标 Apifox / Apipost / Postman 的核心功能，但**没有任何服务端依赖、不需要联网、不上报任何数据**。

## Language

### 数据与存储

**数据根目录 (Data Root)**:
用户指定或默认的一个本地目录，是产品全部数据的容器。根目录内是一个 SQLite 数据库文件 `apidock.db`，所有团队/项目/分组/接口/环境/工作区状态都存于其中。复制根目录（关闭应用后）即可备份或共享全部数据。**默认根目录为 `<用户主目录>/.apidock`**（主目录随操作系统解析：Windows `C:\Users\<用户名>`、macOS/Linux `$HOME`），首次启动自动创建；也可手动切换到其它目录。
_Avoid_: 项目空间

**数据库 (SQLite Database)**:
单一 `apidock.db` 文件，通过 sea-orm（sqlx 后端）访问，WAL 模式。表：`teams` / `projects` / `groups` / `interfaces` / `environments` / `workspace`。分组用 `parent_id` 邻接表表达层级，每项目有一个根分组哨兵行；接口/环境等的复杂嵌套结构（请求体结构树、参数、断言、变量）以 JSON 文本列整体存储。

**团队 (Team)**:
`teams` 表中的一行，`key` 为唯一键，`name` 为显示名（可为中文），用于归属项目（离线场景下即本地的组织/部门）。
_Avoid_: 工作区（旧文档曾用「工作区」指代此层）

**项目 (Project)**:
`projects` 表中的一行，归属某团队，是环境、全局变量/全局参数与接口树的归属单位。`key` 在团队内唯一。
_Avoid_: 集合（旧文档曾用「集合」指代此层）

**分组 (Group)**:
`groups` 表中的一行，项目内可嵌套（`parent_id`），用于按模块/业务再细分接口。`key` 在同父级内唯一。

**接口 (API / Interface)**:
树上的叶子对象，一条可执行的 HTTP 请求定义（方法 + URL + 请求头/查询参数/请求体/鉴权/断言/接口说明）。对应 `interfaces` 表的一行，完整定义存于 `doc` JSON 列。本项目以「接口」为最小单位，接口即请求、可分可测。
_Avoid_: 请求作为正式名词（口语可通，文档统一用「接口」）

**命名规则 (Naming Rule)**:
团队/项目/分组/接口的 `key` 作为数据库唯一键，禁止包含 `\ / : * ? " < > |` 及控制字符（可含中文）；显示名存 `name` 字段，默认与 `key` 相同，可分别修改。

**环境 (Environment)**:
项目级的一个具名变量集合（`environments` 表的一行），含 `host` 与变量列表（默认内置「正式环境/测试环境/开发环境」三套，可删改并支持自定义，内置环境不可删除）。通过「环境 = 当前生效的变量来源」决定请求发往哪里，以项目为单位。

**全局变量 / 全局参数 (Project Globals)**:
以项目为单位的全局配置，存于 `projects` 表的 JSON 列。**全局变量**参与 `{{var}}` 替换；**全局参数**向每个请求注入 Header / Cookie / Query / Body。

**变量 (Variable)**:
`key → value` 数据对，隶属于环境、项目全局或接口。

**变量替换 (Variable Substitution)**:
把 URL、请求头、请求体中的 `{{变量名}}` 模板解析为实际值的过程。生效优先级：**接口级 > 项目全局变量 > 环境变量**；`host` 取自当前激活环境，可按同一优先级被覆盖。

**数据迁移 (Data Migration)**:
首次打开含旧版文件数据（`api-mgmt/` 目录、`workspace.json`）的根目录时，在单个事务内把文件树导入数据库；成功后旧文件归档到 `.file-storage-backup-<时间戳>/`（不删除）。OpenAPI / Postman 的导入导出是独立的数据交换格式，不属于此迁移。

**请求体 (Request Body)**:
请求携带的载荷，支持多种模式：`none`（无）、`json`、`raw`（原始文本）、`urlencoded`（表单）、`form-data`（多段表单）、`file`（文件）。

**鉴权 (Auth)**:
施加到请求上的认证方案，如 `none`、`Bearer Token`、`Basic`、`API Key`、`Digest`、（后续）`OAuth 2.0`。

### 行为与交互

**主窗口 (Main Window)**:
程序启动默认进入的视图：左侧团队列表，右侧选中团队内的项目列表。主窗口为顶部标签栏中最左侧的标签，不可关闭。

**项目主页 (Project Home)**:
点击项目列表中的项目后打开的标签页，展示项目概览（名称、接口数、环境状态等），可从这里进入具体接口。

**发送 (Send)**:
对当前接口执行一次真实 HTTP 调用并展示响应，是接口测试功能的核心动作。

**响应预览 (Response Preview)**:
发送后对响应的展示：状态行、响应头、响应体（JSON 树 / 原始文本 / 耗时 / 体积）。

**断言 (Assertion)**:
挂在接口上、对响应进行校验的规则（状态码、JSONPath、响应头），用于项目/分组一键运行时批量验证。

**导入 (Import)**:
把外部规范文件（OpenAPI 3.0/3.1、Postman Collection 2.x）转换为项目内的接口树。

**导出 (Export)**:
以项目为单位把接口树反向聚合为一份 OpenAPI 3.0/3.1 文档。

### 产品边界

**离线 (Offline)**:
产品的硬性约束：软件运行无需互联网、无账号体系、无遥测、无更新检查（更新由用户手工替换安装包完成）。"离线"描述的是产品能力约束，不代表禁用了对目标内网服务的 HTTP 请求——后者反而是本工具要测试的对象。